/**
 * stats.js
 * (Redacted: GitHub stats theme switching removed by user request)
 * Handles basic interactions if needed in the future.
 */

document.addEventListener('DOMContentLoaded', () => {
    // Current stats are now static in the About section for maximum performance/compactness.
    // Console log to confirm script load if ever needed for debugging.
    console.log("Stats module active (compact mode)");
});
